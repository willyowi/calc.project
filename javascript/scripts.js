var add = function(one,two){
return (one + two);
};
var one =parseInt(prompt("Enter a number"));
alert(one);
var two =parseInt(prompt("Enter another number"));
alert(add(one,two));
var add = function(one,two){
return (one * two);
};
var one =parseInt(prompt("Enter a number"));
alert(one);
var two =parseInt(prompt("Enter another number"));
alert(add(one,two));

var add = function(one,two){
return (one - two);
};
var one =parseInt(prompt("Enter a number"));
alert(one);
var two =parseInt(prompt("Enter another number"));
alert(add(one,two));
var add = function(one,two){
return (one / two);
};
var one =parseInt(prompt("Enter a number"));
alert(one);
var two =parseInt(prompt("Enter another number"));
alert(add(one,two));
var add = function(one,two){
return (one % two);
};
var one =parseInt(prompt("Enter a number"));
alert(one);
var two =parseInt(prompt("Enter another number"));
alert(add(one,two));

